{
  "name": "felix-bot",
  "version": "7.0.0",
  "description": "Felix Bot - AI Assistant with full database integration",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.3.1",
    "groq-sdk": "^0.3.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "vercel dev"
  }
}
