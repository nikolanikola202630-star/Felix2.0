# 🔧 Интеграция v4.1 - Пошаговая инструкция

## 📋 Что нужно сделать

Интегрировать 3 новых сервиса в `api/webhook.js`:
1. Analytics - аналитика использования
2. Cache - кэширование AI ответов
3. Rate Limiter - защита от злоупотреблений

---

## 🚀 Шаг 1: Добавить импорты

В начало файла `api/webhook.js` добавьте:

```javascript
import Groq from 'groq-sdk';
import analytics from '../lib/analytics.js';
import cache from '../lib/cache.js';
import rateLimiter from '../lib/ratelimit.js';

const BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN;
// ... остальной код
```

---

## 🚀 Шаг 2: Добавить Rate Limiting

### В начале обработчика команд

После получения `userId` и `chatId`, добавьте проверку глобального лимита:

```javascript
const chatId = message.chat?.id;
const userId = message.from?.id;
const text = message.text || '';
const voice = message.voice;

if (!chatId || !userId) {
  return res.status(200).json({ ok: true });
}

// ✅ ДОБАВИТЬ: Проверка глобального лимита
const globalLimit = rateLimiter.checkGlobalLimit(userId);
if (!globalLimit.allowed) {
  await sendMessage(chatId, 
    `⏳ Превышен лимит запросов. Попробуйте через ${globalLimit.retryAfter} секунд.`
  );
  return res.status(200).json({ ok: true });
}
```

### Для каждой команды

#### Команда /summary

```javascript
// Handle /summary command
if (text.startsWith('/summary')) {
  // ✅ ДОБАВИТЬ: Проверка лимита
  const limit = rateLimiter.checkLim